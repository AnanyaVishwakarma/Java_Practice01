/*You are asked to search for a specific element in an array. 
An array is a collection of elements (e.g., integers, strings) stored at contiguous memory locations.

Key Objective:
You are given:
An array of elements.
An element to search for (also known as the target).
The task is to check if the element exists in the array. If it does:
Return or print its index (position in the array).
If it doesn’t exist, return or print a message like "Element not found." */

public class searchingAnElement {
    public int searchElem(int[] nums){
        int x;
        int n = nums.length;
        for(int i = 0; i < n; i++){
            
        }
    }   
}
